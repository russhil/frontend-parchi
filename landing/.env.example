# The URL of the main Parchi app (dashboard/login)
# In production, set this to your app subdomain, e.g. https://app.parchi.ai
# If left empty, login links will use relative paths (same domain)
VITE_APP_URL=
